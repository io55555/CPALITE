name: Manual Release Test

on:
  workflow_dispatch:
    inputs:
      release_version:
        description: "Official version, for example v6.9.49"
        required: true
        type: string
      release_notes:
        description: "Enhancement notes for this release"
        required: false
        type: string
      target_ref:
        description: "Optional commit or branch to build"
        required: false
        type: string

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ inputs.release_version }}
      TARGET_REF: ${{ inputs.target_ref || github.sha }}
      FRONTEND_DIR: src/Cli-Proxy-API-Management-Center
      BACKEND_DIR: src/CLIProxyAPI
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ env.TARGET_REF }}

      - name: Set build metadata
        run: |
          echo "COMMIT=$(git rev-parse --short HEAD)" >> "$GITHUB_ENV"
          echo "BUILD_DATE=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_ENV"
          VER="${VERSION#v}"
          echo "VERSION_NUM=$VER" >> "$GITHUB_ENV"
          GO_VER="$(awk '/^go / {print $2; exit}' "$BACKEND_DIR/go.mod")"
          GO_VER="$(printf '%s\n' "$GO_VER" | awk -F. '{print $1"."$2}')"
          echo "GO_ALPINE_IMAGE=golang:${GO_VER}-alpine" >> "$GITHUB_ENV"

      - name: Setup QEMU (for alpine arm64 musl builds)
        uses: docker/setup-qemu-action@v3

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: ${{ env.FRONTEND_DIR }}/package-lock.json

      - name: Build management panel
        working-directory: ${{ env.FRONTEND_DIR }}
        run: |
          npm ci
          VITE_VERSION="$VERSION" npm run build
          cp dist/index.html "$GITHUB_WORKSPACE/management.html"

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: ${{ env.BACKEND_DIR }}/go.mod
          cache-dependency-path: ${{ env.BACKEND_DIR }}/go.sum

      - name: Install CGO toolchains
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            gcc g++ \
            gcc-aarch64-linux-gnu g++-aarch64-linux-gnu \
            zip unzip binutils
      - name: Create release notes
        run: |
          echo "RELEASE_NOTES_FILE=$RUNNER_TEMP/release_notes.md" >> "$GITHUB_ENV"
          RELEASE_NOTES_FILE="$RUNNER_TEMP/release_notes.md"
          cat > "$RELEASE_NOTES_FILE" <<EOF
Official base version: $VERSION
Upstream baselines: see UPSTREAM_BASELINES.md

## Assets
- `CPA_<ver>_linux_<arch>.tar.gz` — default Linux (glibc, CGO=1)
- `CPA_<ver>_linux_<arch>_musl.tar.gz` — Alpine-native musl (CGO=1)
- `CPA_<ver>_linux_<arch>_no-plugin.tar.gz` — portable static Linux (CGO=0; name kept for compatibility)
- `CPA_<ver>_windows_amd64.zip` — Windows
- `management.html` — management panel

## grok-manager
- **Built into the CPA binary** (in-process). No `.so` / `.dll` release assets.
- Enable in `config.yaml`:

```yaml
plugins:
  enabled: true
  configs:
    grok-manager:
      enabled: true
```

- Works on Alpine/musl, glibc, and Windows the same way (config only).
- Optional: `CPA_GROK_MANAGER_BUILTIN=0` forces dynamic library mode if you ship your own `.so` (not recommended).

${{ inputs.release_notes }}
EOF

      - name: Prepare release tag
        run: |
          if git rev-parse "$VERSION" >/dev/null 2>&1; then
            TAG_COMMIT="$(git rev-list -n 1 "$VERSION")"
            HEAD_COMMIT="$(git rev-parse HEAD)"
            if [ "$TAG_COMMIT" != "$HEAD_COMMIT" ]; then
              echo "Tag $VERSION already exists on $TAG_COMMIT, current HEAD is $HEAD_COMMIT" >&2
              exit 1
            fi
          else
            git tag "$VERSION"
          fi

      - name: Ensure clean git tree for GoReleaser
        run: |
          set -euo pipefail
          git status --short || true
          git checkout -- .
          git clean -fd --exclude=management.html --exclude=dist --exclude=src/CLIProxyAPI/dist --exclude=src/Cli-Proxy-API-Management-Center/dist || true
          if [[ -n "$(git status --porcelain --untracked-files=no)" ]]; then
            echo "Tracked files still dirty after cleanup:" >&2
            git status --short --untracked-files=no >&2
            exit 1
          fi
          echo "Tracked worktree is clean for GoReleaser."

      - name: Build non-Linux archives with GoReleaser
        uses: goreleaser/goreleaser-action@v6
        with:
          distribution: goreleaser
          version: '~> v2'
          workdir: ${{ env.BACKEND_DIR }}
          args: release --clean --skip=publish
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Linux/Windows CPA archives (glibc + musl + no-plugin)
        run: |
          # Keep paths relative to repo root; the script normalizes them.
          # Do NOT pre-expand GITHUB_WORKSPACE into BACKEND_DIR and then again into DIST_DIR
          # (bash left-to-right env assignments would double the absolute path).
          VERSION="$VERSION_NUM" \
          COMMIT="$COMMIT" \
          BUILD_DATE="$BUILD_DATE" \
          BACKEND_DIR="$BACKEND_DIR" \
          DIST_DIR="$BACKEND_DIR/dist" \
          GO_ALPINE_IMAGE="$GO_ALPINE_IMAGE" \
          bash .github/scripts/build-cpa-plugin-archives.sh
      - name: Verify release archives
        run: |
          set -euo pipefail
          DIST="$BACKEND_DIR/dist"
          tmp="$(mktemp -d)"
          trap 'rm -rf "$tmp"' EXIT

          check_tar() {
            local archive="$1" expect_dynamic="${2:-1}" expect_musl="${3:-0}"
            local name bin
            name="$(basename "$archive")"
            [[ -f "$archive" ]] || { echo "missing $archive" >&2; exit 1; }
            rm -rf "$tmp/a"; mkdir -p "$tmp/a"
            tar -xzf "$archive" -C "$tmp/a"
            bin="$(find "$tmp/a" -maxdepth 2 -type f -name CPA | head -n1)"
            [[ -n "$bin" ]] || { echo "CPA binary missing in $name" >&2; exit 1; }
            if [[ "$expect_dynamic" == "1" ]]; then
              if ! readelf -l "$bin" | grep -q 'Requesting program interpreter'; then
                echo "$name should be dynamically linked" >&2; exit 1
              fi
            else
              if readelf -l "$bin" | grep -q 'Requesting program interpreter'; then
                echo "$name should be static" >&2; exit 1
              fi
            fi
            if [[ "$expect_musl" == "1" ]]; then
              if ! readelf -l "$bin" | grep -Ei 'musl|ld-musl' >/dev/null; then
                echo "$name does not look like musl" >&2; exit 1
              fi
            fi
            # grok-manager is built into binary; archives must NOT ship .so plugins
            if find "$tmp/a" -type f \( -name 'grok-manager.so' -o -name 'grok-manager.dll' \) | grep -q .; then
              echo "$name unexpectedly contains grok-manager plugin file" >&2
              exit 1
            fi
            echo "ok: $name"
          }

          check_tar "$DIST/CPA_${VERSION_NUM}_linux_amd64.tar.gz" 1 0
          check_tar "$DIST/CPA_${VERSION_NUM}_linux_arm64.tar.gz" 1 0
          check_tar "$DIST/CPA_${VERSION_NUM}_linux_amd64_musl.tar.gz" 1 1
          check_tar "$DIST/CPA_${VERSION_NUM}_linux_arm64_musl.tar.gz" 1 1
          check_tar "$DIST/CPA_${VERSION_NUM}_linux_amd64_no-plugin.tar.gz" 0 0
          check_tar "$DIST/CPA_${VERSION_NUM}_linux_arm64_no-plugin.tar.gz" 0 0
          [[ -f "$DIST/CPA_${VERSION_NUM}_windows_amd64.zip" ]] || { echo "windows zip missing" >&2; exit 1; }
          echo "archive verification passed"

      - name: Create tag when missing

        run: |
          if ! git ls-remote --exit-code --tags origin "refs/tags/$VERSION" >/dev/null 2>&1; then
            git push origin "$VERSION"
          fi

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.VERSION }}
          name: ${{ env.VERSION }}
          body_path: ${{ env.RELEASE_NOTES_FILE }}
          files: |
            management.html
            ${{ env.BACKEND_DIR }}/dist/*.tar.gz
            ${{ env.BACKEND_DIR }}/dist/*.zip
            ${{ env.BACKEND_DIR }}/dist/checksums.txt

      - name: Keep only latest 10 releases
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          mapfile -t releases < <(gh release list --limit 200 --json tagName,createdAt --jq 'sort_by(.createdAt) | reverse | .[].tagName')
          total="${#releases[@]}"
          echo "Found ${total} releases"
          if (( total <= 10 )); then
            echo "No pruning needed (limit 10)"
            exit 0
          fi
          for (( i=10; i<total; i++ )); do
            tag="${releases[$i]}"
            echo "Deleting old release: ${tag}"
            gh release delete "${tag}" --yes --cleanup-tag || true
          done
