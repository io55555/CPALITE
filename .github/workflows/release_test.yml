name: Manual Release Test

on:
  workflow_dispatch:
    inputs:
      release_version:
        description: "Official version, for example v6.9.49"
        required: true
        type: string
      release_notes:
        description: "Enhancement notes for this release"
        required: false
        type: string
      target_ref:
        description: "Optional commit or branch to build"
        required: false
        type: string

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ inputs.release_version }}
      TARGET_REF: ${{ inputs.target_ref || github.sha }}
      FRONTEND_DIR: src/Cli-Proxy-API-Management-Center
      BACKEND_DIR: src/CLIProxyAPI
      # Local plugin source (do not build from upstream-original trees)
      GROK_MANAGER_DIR: src/grok-manager
      GROK_MANAGER_VERSION: "1.3.7"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ env.TARGET_REF }}

      - name: Set build metadata
        run: |
          echo "COMMIT=$(git rev-parse --short HEAD)" >> "$GITHUB_ENV"
          echo "BUILD_DATE=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_ENV"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: ${{ env.FRONTEND_DIR }}/package-lock.json

      - name: Build management panel
        working-directory: ${{ env.FRONTEND_DIR }}
        run: |
          npm ci
          VITE_VERSION="$VERSION" npm run build
          cp dist/index.html "$GITHUB_WORKSPACE/management.html"

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: ${{ env.BACKEND_DIR }}/go.mod
          cache-dependency-path: |
            ${{ env.BACKEND_DIR }}/go.sum
            ${{ env.GROK_MANAGER_DIR }}/go.mod

      - name: Install CGO cross compilers for grok-manager
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc g++ gcc-aarch64-linux-gnu g++-aarch64-linux-gnu gcc-mingw-w64-x86-64 g++-mingw-w64-x86-64 zip unzip

      - name: Build grok-manager plugin (linux/windows)
        run: |
          # Use bash instead of chmod so tracked file modes stay clean for GoReleaser.
          PLUGIN_SRC="$GITHUB_WORKSPACE/${GROK_MANAGER_DIR}" \
          OUT_ROOT="$GITHUB_WORKSPACE/dist/plugins" \
          GROK_MANAGER_VERSION="$GROK_MANAGER_VERSION" \
          bash .github/scripts/build-grok-manager.sh

      - name: Create release notes
        run: |
          echo "RELEASE_NOTES_FILE=$RUNNER_TEMP/release_notes.md" >> "$GITHUB_ENV"
          RELEASE_NOTES_FILE="$RUNNER_TEMP/release_notes.md"
          cat > "$RELEASE_NOTES_FILE" <<EOF
          Official base version: $VERSION
          Upstream baselines: see UPSTREAM_BASELINES.md
          Includes: management.html, CLIProxyAPI multi-platform archives, grok-manager plugin (where built), checksums.txt
          Plugin: grok-manager v$GROK_MANAGER_VERSION (proxy: auth-file > CPA proxy-url > direct)

          ${{ inputs.release_notes }}
          EOF

      - name: Prepare release tag
        run: |
          if git rev-parse "$VERSION" >/dev/null 2>&1; then
            TAG_COMMIT="$(git rev-list -n 1 "$VERSION")"
            HEAD_COMMIT="$(git rev-parse HEAD)"
            if [ "$TAG_COMMIT" != "$HEAD_COMMIT" ]; then
              echo "Tag $VERSION already exists on $TAG_COMMIT, current HEAD is $HEAD_COMMIT" >&2
              exit 1
            fi
          else
            git tag "$VERSION"
          fi

      - name: Ensure clean git tree for GoReleaser
        run: |
          # GoReleaser rejects dirty tracked files. Keep untracked build artifacts.
          git status --short || true
          git checkout -- .
          git status --short || true

      - name: Build archives with GoReleaser
        uses: goreleaser/goreleaser-action@v6
        with:
          distribution: goreleaser
          version: '~> v2'
          workdir: ${{ env.BACKEND_DIR }}
          args: release --clean --skip=publish
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Inject grok-manager into CPA archives
        run: |
          BACKEND_DIR="$GITHUB_WORKSPACE/${BACKEND_DIR}" \
          PLUGIN_ROOT="$GITHUB_WORKSPACE/dist/plugins" \
          bash .github/scripts/package-grok-manager.sh

      - name: Create tag when missing
        run: |
          if ! git ls-remote --exit-code --tags origin "refs/tags/$VERSION" >/dev/null 2>&1; then
            git push origin "$VERSION"
          fi

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.VERSION }}
          name: ${{ env.VERSION }}
          body_path: ${{ env.RELEASE_NOTES_FILE }}
          files: |
            management.html
            ${{ env.BACKEND_DIR }}/dist/*.tar.gz
            ${{ env.BACKEND_DIR }}/dist/*.zip
            ${{ env.BACKEND_DIR }}/dist/checksums.txt
            dist/plugins/linux/amd64/grok-manager.so
            dist/plugins/linux/amd64/grok-manager-v${{ env.GROK_MANAGER_VERSION }}.so
            dist/plugins/linux/arm64/grok-manager.so
            dist/plugins/linux/arm64/grok-manager-v${{ env.GROK_MANAGER_VERSION }}.so
            dist/plugins/windows/amd64/grok-manager.dll
            dist/plugins/windows/amd64/grok-manager-v${{ env.GROK_MANAGER_VERSION }}.dll

      - name: Keep only latest 10 releases
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          mapfile -t releases < <(gh release list --limit 200 --json tagName,createdAt --jq 'sort_by(.createdAt) | reverse | .[].tagName')
          total="${#releases[@]}"
          echo "Found ${total} releases"
          if (( total <= 10 )); then
            echo "No pruning needed (limit 10)"
            exit 0
          fi
          for (( i=10; i<total; i++ )); do
            tag="${releases[$i]}"
            echo "Deleting old release: ${tag}"
            gh release delete "${tag}" --yes --cleanup-tag || true
          done